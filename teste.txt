program IMC

num altura;
num peso,
num resultado;
char texto;

{
	if(a){
	}else{
	}
   write "Digite seu peso:";
   read peso;
   write "Digite sua altura:"
   read altura;

   resultado = peso / (altura altura);

   write "Seu IMC é:";
   write resultado;
   write "Digite seu peso:";
   read peso;
   write "Digite sua altura:";
   read altura;

   resultado = peso / (altura * altura);

   write "Seu IMC é:";
   write resultado;
}
